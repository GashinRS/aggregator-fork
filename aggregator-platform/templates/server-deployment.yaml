apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregator-server
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aggregator.labels" $ | nindent 4 }}
    app.kubernetes.io/component: server
  annotations:
    {{- include "aggregator.annotations" $ | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicaCount }}
  selector:
    matchLabels:
      {{- include "aggregator.labels" $ | nindent 6 }}
      app.kubernetes.io/component: server
  template:
    metadata:
      labels:
        {{- include "aggregator.labels" $ | nindent 8 }}
        app.kubernetes.io/component: server
      annotations:
        {{- include "aggregator.annotations" $ | nindent 8 }}
    spec:
      serviceAccountName: aggregator-server-sa
      containers:
        - name: aggregator-server
          image: "{{ .Values.server.image.repository }}:{{ .Values.server.image.tag }}"
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: server-config
            - configMapRef:
                name: spec-config
          {{- if .Values.trust.ca.enabled }}
          env:
            - name: SSL_CERT_FILE
              value: {{ printf "%s/%s" .Values.trust.ca.mountPath .Values.trust.ca.key | quote }}
            - name: NODE_EXTRA_CA_CERTS
              value: {{ printf "%s/%s" .Values.trust.ca.mountPath .Values.trust.ca.key | quote }}
          {{- end }}
          {{- if .Values.trust.ca.enabled }}
          volumeMounts:
            - name: pacsoi-ca
              mountPath: {{ .Values.trust.ca.mountPath | quote }}
              readOnly: true
          {{- else if or .Values.auth.oidc.clientSecret .Values.auth.client_credentials.clientSecret }}
          volumeMounts:
          {{- end }}
          {{- if or .Values.auth.oidc.clientSecret .Values.auth.client_credentials.clientSecret }}
            - name: client-secrets
              mountPath: /etc/secrets
              readOnly: true
          {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: {{ .Values.server.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.server.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.server.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.server.readinessProbe.failureThreshold }}
      {{- if or .Values.trust.ca.enabled .Values.auth.oidc.clientSecret .Values.auth.client_credentials.clientSecret }}
      volumes:
      {{- if .Values.trust.ca.enabled }}
        - name: pacsoi-ca
          configMap:
            name: {{ .Values.trust.ca.configMapName }}
      {{- end }}
      {{- if or .Values.auth.oidc.clientSecret .Values.auth.client_credentials.clientSecret }}
        - name: client-secrets
          secret:
            secretName: aggregator-server-secret
      {{- end }}
      {{- end }}
